# Optional: GitHub Personal Access Token (classic or fine-grained with repo read access)
# Used at build time to fetch recent commits including private repos without rate limits.
GITHUB_TOKEN=your_github_token_here
